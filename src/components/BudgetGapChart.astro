---
// Budget Gap Chart Component
---

<div class="chart-container">
  <div class="chart-header">
    <h3>The $2 Trillion Problem</h3>
    <p class="chart-subtitle">Annual Government Spending vs Tax Revenue (2025 Projected)</p>
  </div>
  <div class="chart-wrapper">
    <canvas id="budgetChart" width="400" height="300"></canvas>
  </div>
  <div class="chart-insight">
    <div class="insight-icon">💡</div>
    <div class="insight-content">
      <strong>Key Insight:</strong> The government spends $7 trillion but only collects $5 trillion in taxes. 
      That $2 trillion gap gets added to our national debt every single year.
    </div>
  </div>
</div>

<style>
  .chart-container {
    background: rgba(254, 254, 254, 0.9);
    border-radius: 16px;
    padding: 2rem;
    margin: 2rem 0;
    border: 1px solid rgba(90, 107, 93, 0.15);
    box-shadow: 0 4px 20px rgba(45, 45, 45, 0.06);
    backdrop-filter: blur(10px);
  }

  .chart-header {
    text-align: center;
    margin-bottom: 2rem;
  }

  .chart-header h3 {
    font-family: 'Playfair Display', serif;
    font-size: 1.5rem;
    font-weight: 600;
    color: var(--charcoal-dark);
    margin-bottom: 0.5rem;
  }

  .chart-subtitle {
    color: var(--shadow-subtle);
    font-size: 0.95rem;
    font-style: italic;
  }

  .chart-wrapper {
    position: relative;
    height: 300px;
    margin-bottom: 1.5rem;
  }

  .chart-insight {
    display: flex;
    align-items: flex-start;
    gap: 1rem;
    background: linear-gradient(135deg, var(--cream-warm) 0%, var(--mist-light) 100%);
    padding: 1.5rem;
    border-radius: 12px;
    border-left: 4px solid var(--sage-green);
  }

  .insight-icon {
    font-size: 1.5rem;
    flex-shrink: 0;
  }

  .insight-content {
    font-size: 0.95rem;
    line-height: 1.6;
    color: var(--charcoal-light);
  }

  .insight-content strong {
    color: var(--steel-blue);
  }
</style>

<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<script>
  document.addEventListener('DOMContentLoaded', function() {
    const ctx = document.getElementById('budgetChart');
    if (ctx && typeof Chart !== 'undefined') {
      new Chart(ctx, {
        type: 'doughnut',
        data: {
          labels: ['Tax Revenue ($5T)', 'Additional Borrowing ($2T)'],
          datasets: [{
            data: [5, 2],
            backgroundColor: [
              'rgba(90, 107, 93, 0.8)',  // Sage green
              'rgba(220, 53, 69, 0.8)'   // Red for deficit
            ],
            borderColor: [
              'rgb(90, 107, 93)',
              'rgb(220, 53, 69)'
            ],
            borderWidth: 2,
            cutout: '60%'
          }]
        },
        options: {
          responsive: true,
          maintainAspectRatio: false,
          plugins: {
            legend: {
              position: 'bottom',
              labels: {
                padding: 20,
                font: {
                  size: 14
                },
                color: '#4A4A4A'
              }
            },
            tooltip: {
              callbacks: {
                label: function(context) {
                  const label = context.label || '';
                  const value = context.parsed;
                  return `${label}: $${value} trillion`;
                }
              }
            }
          }
        }
      });
    }
  });
</script>
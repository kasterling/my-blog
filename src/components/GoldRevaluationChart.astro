---
// Gold Revaluation Comparison Chart
---

<div class="chart-container">
  <div class="chart-header">
    <h3>The Hidden Gold Treasure</h3>
    <p class="chart-subtitle">Government Gold Value: Book Price vs Market Price</p>
  </div>
  <div class="chart-wrapper">
    <canvas id="goldChart" width="400" height="300"></canvas>
  </div>
  <div class="chart-stats">
    <div class="stat-item">
      <div class="stat-value">$11B</div>
      <div class="stat-label">Current Book Value<br><span class="stat-detail">($42.22/oz since 1973)</span></div>
    </div>
    <div class="stat-arrow">→</div>
    <div class="stat-item highlight">
      <div class="stat-value">$688B</div>
      <div class="stat-label">Market Value Today<br><span class="stat-detail">($2,633/oz)</span></div>
    </div>
  </div>
  <div class="chart-insight">
    <div class="insight-icon">💡</div>
    <div class="insight-content">
      <strong>Key Insight:</strong> The government could "find" $677 billion just by updating 50-year-old accounting. 
      That's enough to fund the government for about 5 weeks!
    </div>
  </div>
</div>

<style>
  .chart-container {
    background: rgba(254, 254, 254, 0.9);
    border-radius: 16px;
    padding: 2rem;
    margin: 2rem 0;
    border: 1px solid rgba(90, 107, 93, 0.15);
    box-shadow: 0 4px 20px rgba(45, 45, 45, 0.06);
    backdrop-filter: blur(10px);
  }

  .chart-header {
    text-align: center;
    margin-bottom: 2rem;
  }

  .chart-header h3 {
    font-family: 'Playfair Display', serif;
    font-size: 1.5rem;
    font-weight: 600;
    color: var(--charcoal-dark);
    margin-bottom: 0.5rem;
  }

  .chart-subtitle {
    color: var(--shadow-subtle);
    font-size: 0.95rem;
    font-style: italic;
  }

  .chart-wrapper {
    position: relative;
    height: 300px;
    margin-bottom: 2rem;
  }

  .chart-stats {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 2rem;
    margin-bottom: 2rem;
    padding: 1.5rem;
    background: linear-gradient(135deg, var(--warm-light) 0%, var(--cream-warm) 100%);
    border-radius: 12px;
  }

  .stat-item {
    text-align: center;
    flex: 1;
  }

  .stat-value {
    font-family: 'Playfair Display', serif;
    font-size: 2.5rem;
    font-weight: 700;
    color: var(--charcoal-dark);
    margin-bottom: 0.5rem;
  }

  .stat-label {
    font-size: 0.9rem;
    color: var(--shadow-subtle);
    font-weight: 500;
  }

  .stat-detail {
    font-size: 0.8rem;
    color: var(--steel-blue);
    font-style: italic;
  }

  .stat-arrow {
    font-size: 2rem;
    color: var(--sage-green);
    font-weight: bold;
  }

  .highlight .stat-value {
    color: var(--sage-green);
  }

  .chart-insight {
    display: flex;
    align-items: flex-start;
    gap: 1rem;
    background: linear-gradient(135deg, var(--cream-warm) 0%, var(--mist-light) 100%);
    padding: 1.5rem;
    border-radius: 12px;
    border-left: 4px solid var(--sage-green);
  }

  .insight-icon {
    font-size: 1.5rem;
    flex-shrink: 0;
  }

  .insight-content {
    font-size: 0.95rem;
    line-height: 1.6;
    color: var(--charcoal-light);
  }

  .insight-content strong {
    color: var(--steel-blue);
  }

  @media (max-width: 768px) {
    .chart-stats {
      flex-direction: column;
      gap: 1rem;
    }

    .stat-arrow {
      transform: rotate(90deg);
    }

    .stat-value {
      font-size: 2rem;
    }
  }
</style>

<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<script>
  document.addEventListener('DOMContentLoaded', function() {
    const ctx = document.getElementById('goldChart');
    if (ctx && typeof Chart !== 'undefined') {
      new Chart(ctx, {
        type: 'bar',
        data: {
          labels: ['Book Value (1973 Price)', 'Current Market Value'],
          datasets: [{
            label: 'Value in Billions ($)',
            data: [11, 688],
            backgroundColor: [
              'rgba(220, 53, 69, 0.7)',   // Red for outdated book value
              'rgba(90, 107, 93, 0.8)'    // Sage green for market value
            ],
            borderColor: [
              'rgb(220, 53, 69)',
              'rgb(90, 107, 93)'
            ],
            borderWidth: 2,
            borderRadius: 8
          }]
        },
        options: {
          responsive: true,
          maintainAspectRatio: false,
          plugins: {
            legend: {
              display: false
            },
            tooltip: {
              callbacks: {
                label: function(context) {
                  return `$${context.parsed.y} billion`;
                }
              }
            }
          },
          scales: {
            y: {
              beginAtZero: true,
              ticks: {
                callback: function(value) {
                  return '$' + value + 'B';
                },
                color: '#4A4A4A'
              },
              grid: {
                color: 'rgba(0,0,0,0.1)'
              }
            },
            x: {
              ticks: {
                color: '#4A4A4A',
                maxRotation: 45
              },
              grid: {
                display: false
              }
            }
          }
        }
      });
    }
  });
</script>